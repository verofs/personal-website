BLOB_READ_WRITE_TOKEN=
# Generate with:
# node -e "const {scryptSync,randomBytes}=require('crypto');const p=process.argv[1];const s=randomBytes(16);console.log('scrypt:'+s.toString('base64')+':'+scryptSync(p,s,64).toString('base64'))" "replace-with-password"
ADMIN_PASSWORD_HASH=scrypt:base64Salt:base64Hash
ADMIN_SESSION_SECRET=
